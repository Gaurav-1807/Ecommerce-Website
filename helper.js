// const { login } = require("./controllars/user.logic")

const Getpath = () => {
    // let path = __dirname;
    // console.log(path);

    // // Replace backslashes with forward slashes
    // let newPath = path.replace(/\\/g, "/");

    // console.log(newPath);
    return __dirname
}

// Call the function
Getpath();


Getpath()

module.exports=Getpath